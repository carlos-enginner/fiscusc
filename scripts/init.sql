-- Fiscus-C - Inicialização do banco de dados
-- Executado automaticamente pelo PostgreSQL na primeira inicialização

CREATE EXTENSION IF NOT EXISTS vector;
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
